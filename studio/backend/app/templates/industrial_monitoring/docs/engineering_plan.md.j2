# Engineering Team Plan

This project was generated by the Black Dragon Studio engineering-team pipeline.
Each role reviewed the approved `system_spec.json` before files were exported.
The full machine-readable handoff is available in `engineering_review.json`.

{% for agent in engineering_agents %}
## {{ agent.role }}

**Responsibility:** {{ agent.responsibility }}

**Outputs**
{% for output in agent.outputs %}- {{ output }}
{% endfor %}
{% if agent.handoff_to %}
**Handoff:** {{ agent.role }} → {{ agent.handoff_to }}
{% else %}
**Handoff:** final export package
{% endif %}

{% endfor %}
## Validation gates

The generated project is considered export-ready only after schema validation,
static checks, unit tests, simulation smoke testing, and package validation pass.
