"""{{ spec.project.name }} -- generated by Black Dragon Studio.

Domain: {{ spec.project.domain }}
Target platform: {{ spec.project.target_platform }}

This project was produced from an approved SystemSpec (see
system_spec.json at the project root) using the "industrial_monitoring"
template. See generation_manifest.json for template version and
generation time.

LABEL: all simulation results in this project are SIMULATED, not measured
on real hardware. See README.md "Honest Claims" section.
"""

__version__ = "0.1.0"
